/**
 * 可选：在 `minecraft-data` 的 `supportedVersions.bedrock` 之外再合并进下拉与 `resolveBedrockVersion` 候选。
 * 当前版本列表已由依赖内的 mc-data 提供（含 `26.10` / 944），此处留空即可。
 */
export const EXTRA_BEDROCK_VERSIONS: string[] = [];
